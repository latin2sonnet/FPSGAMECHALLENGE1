services:
  - type: web
    name: neon-strike-server
    runtime: node
    plan: standard
    buildCommand: npm install && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
